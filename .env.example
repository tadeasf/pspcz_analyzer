# PSP.cz Analyzer — Environment Variables
# Copy this file to .env and adjust values as needed.
# All variables are optional — defaults work for local development.

# --- Cache ---
# Directory for downloaded data, parquet caches, PDF texts, etc.
# PSPCZ_CACHE_DIR=~/.cache/pspcz-analyzer/psp

# --- Dev mode ---
# Set to "1" to enable hot reload (uvicorn --reload). Default: "1" (dev mode).
# PSPCZ_DEV=1

# --- Ollama (LLM integration) ---
# Base URL for the Ollama API. Local default: http://localhost:11434
# For a remote HTTPS instance, set the full URL (e.g. https://ollama.example.com).
# OLLAMA_BASE_URL=http://localhost:11434

# Bearer token for Ollama API authentication (required for remote/cloud Ollama).
# Leave empty or unset for local Ollama (no auth needed).
# OLLAMA_API_KEY=

# Model name for topic classification and summarization.
# OLLAMA_MODEL=qwen3:8b
