## Summary

<!-- What does this PR change, and why? -->

## Linked issue

<!-- e.g. Closes #123 -->

## Checklist

- [ ] Tests added/updated for the change
- [ ] `uv run ruff format .`, `uv run ruff check --fix .`, `uv run pytest -m "not integration" --cov -q`, and `uv run pyright` all pass
- [ ] Docs / README updated if user-visible behavior changed
- [ ] New UI strings added to `i18n/translations.py` in **both** `cs` and `en`
